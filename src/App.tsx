import { Card } from 'flowbite-react';
import Layout from './interface/layout';

const App = () => {
    return (
        <Layout>
            <Card>home</Card>
        </Layout>
    );
};

export default App;
